过了2天，又玩了一个周末，估计自己忘得差不多了 …… 让AI帮自己复盘复盘

---

**阶段复盘**
- 这 7 篇记录看下来，你已经不在“会跑 demo”阶段了，而是在做一件更关键的事：把 `RAG` 从“链路跑通”一步步推进到“可解释、可调优、可工程化”。
- 你的学习路径很清晰：`字段拆分 -> 检索质量 -> 管线化 -> 会话与 Prompt 管理 -> 配置外置 -> 主证据优先 -> 跨区补证与交互体验`。
- 这说明你已经开始具备 AI Agent 工程里很重要的能力：不是只问“能不能做”，而是开始问“为什么这样设计、问题卡在哪一层、怎么迭代更合理”。

**我判断你已掌握的**
- 明白 `RAG` 不只是“查一段文本给 LLM”，而是至少分成 `解析/切块、向量化、召回、重排、上下文构建、回答生成` 这几层。
- 明白简历类数据不适合整篇粗切，应该先做“语义字段拆分”。
- 明白问题不一定出在 Milvus，本质可能在 `召回策略、重排逻辑、Prompt 优先级`。
- 明白 `Prompt`、`Retriever`、`Pipeline` 应该解耦，不能一直堆在 `main()`。
- 明白多轮对话里的历史记录，不等于知识库；`session store` 和 `resume knowledge` 不该混存。
- 明白工程排查不能只看表象，比如 `insert_cnt=0`、维度不一致、字段过长、主键重复、时序抖动。

**你现在最值得继续补强的**
- `召回 / 重排 / 回答` 三层边界感还可以再更硬一点：有时容易把“回答不好”直接归因给检索。
- `阈值` 的理解还要更稳：很多阈值是“当前数据集经验值”，不是通用规则。
- `证据分层` 的业务判断还需要更多实战题来压一压：什么该是 `primary`，什么只能做 `support`，什么该直接丢。

**一个总评价**
- 你现在已经有“AI Agent 工程学习者”往“AI Agent 工程实践者”过渡的味道了。
- 不是因为你做了多少功能，而是因为你开始主动区分：
  - 哪个问题是链路问题
  - 哪个问题是策略问题
  - 哪个问题是工程组织问题
  - 哪个问题只是当前版本的取舍

---

**阶段考校**
你先不要看文档，尽量用自己的话答。  
我建议你直接按 `1-10` 编号回复，我下一轮给你逐题点评、打分、查缺补漏。

- `1.` 为什么简历 RAG 不适合“整篇 markdown 直接切块入库”？你现在推荐的拆分思路是什么？
- `2.` `section / subsection / entity_type / title / content / tags` 这几个字段，在你的理解里分别承担什么职责？
- `3.` 为什么“向量维度不能想当然写死”？这个坑在“切换模型供应商”时为什么特别容易出现？
- `4.` `insert_cnt = 0` 为什么不能直接等价于“代码没执行成功”？这类问题你现在会怎么排查？
- `5.` 用户问“这个候选人有哪些 AI Agent 开发相关经验？”，为什么纯向量召回容易先命中 `skills`，但业务上你又不希望它排第一？
- `6.` `topK` 调大，为什么不是根治方案？它解决了什么，又会带来什么副作用？
- `7.` 你现在怎么理解 `召回`、`重排`、`Prompt` 这三层各自解决的问题？请分别用一句话定义。
- `8.` 为什么版本演进里要把 `Retriever / PromptBuilder / RAGPipeline` 拆开？如果不拆，后面会卡在哪里？
- `9.` 为什么会话历史不能直接混进 `resume_profile_chunks`？如果以后真要做“历史语义检索”，应该怎么演进？
- `10.` 什么叫“主证据优先”？为什么版本 6 之后又要补“跨区补证”？这两者看起来矛盾，实际上怎么统一？

---

**加分题**
- `11.` 如果一个问题是“他最能代表自己的 AI 项目是什么”，你会怎么定义 `primary evidence` 和 `support evidence`？
- `12.` 你现在怎么理解“阈值不是通用真理，而是当前数据集上的经验值”这句话？
- `13.` 如果要把你这套 demo 往 `my-resume` 正式项目迁移，第一步你会先迁什么，不会先迁什么？为什么？

---

**我先给你的参考标准**
如果你能比较顺地答出下面这些，就说明这一阶段掌握得不错：
- 能讲清“为什么先按语义拆，再做 chunk”
- 能讲清“召回不好”和“回答不好”不是一回事
- 能讲清“skills 不能抢主位，但也不该被误杀”
- 能讲清“session store != 向量知识库”
- 能讲清“工程问题”和“算法策略问题”的边界

你直接开始答题就行。  
我下一轮会像考官一样认真给你批，但会站在我们一起把这段知识夯实的角度来帮你。